[package]
name = "next-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
next = { path = "../next" }
toml = "0.8"
serde = { version = "1", features = ["derive"] }
git2 = "0.19"
uuid = { version = "1", features = ["serde"] }

[dev-dependencies]
tempfile = "3"
chrono = "0.4"
